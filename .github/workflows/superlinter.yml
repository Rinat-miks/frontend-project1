name: Super-Linter

on: push

jobs:
  eslint:
    name: Lint code base
    runs-on: ubuntu-latest

    steps:

      - name: install
        run: make install
      
      - name: eslint
        run: make lint